const usernameField = document.getElementsByName("username")[0]
const passwordField = document.getElementsByName("password")[0]
const button = document.querySelector(".body form button")

button.addEventListener("click", () => {
    if (condition) {
        
    }
})
